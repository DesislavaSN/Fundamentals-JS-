// A function, that checks whether the result of the multiplication numOne * numTwo * numThree is positive or negative.

function signCheck(num1, num2, num3) {
    if (num1 * num2 * num3 > 0) {
        return ("Positive");
    } else {
        return ("Negative");
    }
}
