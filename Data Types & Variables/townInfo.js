// A function which prints a town information from the given parameters.

function townInfo(townName, population , areaNum){
    console.log(`Town ${townName} has population of ${population} and area ${areaNum} square km.`);
}
